# CLAUDE.md

Claude-specific instructions for this repository.

## Git workflow

`AGENTS.md` is the source of truth for repository-wide Git conventions — branch naming, agent behavior before starting a task, the parallel worktree workflow, and commit/PR expectations. Follow it before creating or switching branches, and before committing.

For the exact commit message format required by this repo (subject rules; no Jira ticket reference — this repo doesn't use one), see `.claude/commands/commit.md`.
