const Fonts = {
  // These keys must match the names passed to `useFonts` in `app/_layout.tsx`.
  regular: "SF-Compact-Rounded-Regular",
  medium: "SF-Compact-Rounded-Medium",
  semibold: "SF-Compact-Rounded-Semibold",
  bold: "SF-Compact-Rounded-Bold",

  // Optional mono font kept for code/labels if you use it.
  mono: "SpaceMono",
};

export default Fonts;